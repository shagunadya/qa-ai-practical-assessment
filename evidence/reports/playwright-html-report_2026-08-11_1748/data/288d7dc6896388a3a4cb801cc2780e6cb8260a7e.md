# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api\regression\invalid-login.api.spec.js >> API invalid login @regression >> POST users login rejects wrong password @regression
- Location: PrismStructure\tests\api\regression\invalid-login.api.spec.js:6:3

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 401
Received: 423
```

# Test source

```ts
  1  | const { test, expect } = require('@playwright/test');
  2  | const apiData = require('../../../data/api-test-data');
  3  | const { ToolshopApiClient } = require('../../../api/ToolshopApiClient');
  4  | 
  5  | test.describe('API invalid login @regression', () => {
  6  |   test('POST users login rejects wrong password @regression', async ({
  7  |     request,
  8  |   }) => {
  9  |     const client = new ToolshopApiClient(request);
  10 |     const response = await client.login(
  11 |       apiData.invalidLogin.email,
  12 |       apiData.invalidLogin.password,
  13 |     );
  14 | 
> 15 |     expect(response.status()).toBe(401);
     |                               ^ Error: expect(received).toBe(expected) // Object.is equality
  16 |     expect(client.token).toBeNull();
  17 |   });
  18 | });
  19 | 
```